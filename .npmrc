# pnpm configuration for beyond-chatbot project
# Force use of pnpm as the package manager

engine-strict=true

# Store location (optional, can be customized per local setup)
# store-dir=./.pnpm-store
